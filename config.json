{
  "_comment": "Runtime knobs for tx-county-watch. Cadence is NOT hardcoded in the pipeline; the scheduler (cron / GitHub Actions) reads these to decide frequency. Flip election_window to true to switch to the higher-frequency schedule during the election window.",
  "election_window": false,
  "cadence": {
    "baseline_cron": "0 8 * * *",
    "election_window_cron": "0 */3 * * *",
    "note": "baseline = daily 08:00 UTC; election window = every 3 hours. Adjust freely."
  },
  "fetch": {
    "js_shell_min_chars": 500,
    "plain_timeout_seconds": 30,
    "headless_timeout_ms": 45000,
    "plain_retries": 2,
    "hydration_settle_ms": 2000,
    "hydration_max_wait_ms": 6000
  }
}
