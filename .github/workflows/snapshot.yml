name: snapshot

# Cadence is not hardcoded into the pipeline. Two schedules are defined here:
#   - a DAILY baseline that always runs
#   - an ELECTION-WINDOW cron (every 3h) that only does work when EITHER the
#     repository variable ELECTION_WINDOW == 'true' (Settings -> Secrets and
#     variables -> Actions -> Variables) OR today falls inside the automatic
#     high-cadence window declared in the gate step below.
#
# The variable is the manual override and still works in both directions. The
# date window exists so the general election does not depend on somebody
# remembering to flip it: the March primary was captured at daily cadence only
# because nobody thought to raise it, and November should not be decided the
# same way.
#
# The window dates are Texas's, NOT a copy of fl-county-watch's. The two repos
# share an election day and therefore share a takedown period, but their early
# voting periods differ by statute, so their windows open on different days.
# Adjust the cron strings or the window dates to taste; keep the gating pattern.

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 8 * * *"      # daily baseline, 08:00 UTC
    - cron: "0 */3 * * *"    # election-window cadence, every 3 hours (gated)

concurrency:
  group: snapshot
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  snapshot:
    # The frequent cron is admitted to the job and then decided by the gate step,
    # because an `if:` expression here has no access to the current date. The cost
    # is a short runner start every 3 hours outside the window; the alternative is
    # having no automatic high-cadence period at all.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.schedule == '0 8 * * *' ||
      github.event.schedule == '0 */3 * * *'
    runs-on: ubuntu-latest
    # At 254 counties a run is ~1,500 targets across six page types. With the
    # plain path running concurrently that lands well under an hour, but the cap
    # keeps a hung fetch from burning the 6-hour default (and, on a private repo,
    # billable minutes).
    timeout-minutes: 120
    steps:
      - name: Decide whether this run captures
        id: gate
        env:
          EVENT: ${{ github.event_name }}
          SCHEDULE: ${{ github.event.schedule }}
          ELECTION_WINDOW: ${{ vars.ELECTION_WINDOW }}
        run: |
          set -euo pipefail

          # Automatic high-cadence window, UTC, inclusive of both endpoints.
          #
          # Start: Texas early voting by personal appearance for the 2026 general
          # runs Oct 19 - Oct 30 (Election Code 85.001 puts it on the 17th day
          # before election day, which is Sat Oct 17, so it slides to the next
          # business day; it ends on the 4th day before). Opening Oct 15 catches
          # the ramp into it, the same four-day lead fl-county-watch uses.
          #
          # This is five days EARLIER than Florida's Oct 20, because Florida's
          # mandatory early voting does not begin until Oct 24. Do not sync these
          # two numbers -- they are answers to different statutes.
          #
          # End: the post-election takedown curve is the point of the exercise and
          # it lives in the WEEKS AFTER Nov 3, not on election night. This one DOES
          # match Florida's, because election day is the same in both states.
          WINDOW_START=20261015
          WINDOW_END=20261117

          today=$(date -u +%Y%m%d)
          go=false

          if [ "$EVENT" = "workflow_dispatch" ]; then
            go=true; why="manual dispatch"
          elif [ "$SCHEDULE" = "0 8 * * *" ]; then
            go=true; why="daily baseline"
          elif [ "${ELECTION_WINDOW:-}" = "true" ]; then
            go=true; why="ELECTION_WINDOW=true (manual override)"
          elif [ "$today" -ge "$WINDOW_START" ] && [ "$today" -le "$WINDOW_END" ]; then
            go=true; why="inside automatic window ${WINDOW_START}..${WINDOW_END}"
          else
            why="skipped: outside ${WINDOW_START}..${WINDOW_END} and ELECTION_WINDOW is '${ELECTION_WINDOW:-unset}'"
          fi

          echo "go=$go" >> "$GITHUB_OUTPUT"
          echo "capture=$go — $why"
          echo "### snapshot gate: \`$go\`" >> "$GITHUB_STEP_SUMMARY"
          echo "$why" >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/checkout@v4
        if: steps.gate.outputs.go == 'true'

      - uses: actions/setup-python@v5
        if: steps.gate.outputs.go == 'true'
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.gate.outputs.go == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Run snapshot (fetch -> normalize -> write -> commit)
        if: steps.gate.outputs.go == 'true'
        run: |
          git config user.name "tx-county-watch[bot]"
          git config user.email "actions@github.com"
          python scripts/snapshot.py

      # If the run died part-way (flaky county, network blip), pick up where it
      # left off rather than refetching everything.
      - name: Resume once if the run failed
        if: failure() && steps.gate.outputs.go == 'true'
        run: python scripts/snapshot.py --resume

      - name: Push snapshot commit
        if: steps.gate.outputs.go == 'true'
        run: git push
